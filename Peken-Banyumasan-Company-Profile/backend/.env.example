# Company Profile backend — salin jadi `.env` lalu isi nilai rahasia dari .env tim.
# PENTING: SUPABASE_DB_URL WAJIB berprefix `postgresql+asyncpg://` —
# dengan `postgresql://` biasa SEMUA request menjadi 500.
SUPABASE_DB_URL=postgresql+asyncpg://postgres.<project>:<password>@aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres
SUPABASE_URL=https://qelxhftrvwrjfraulwgx.supabase.co
SUPABASE_KEY=<anon-key — salin dari .env tim>
SUPABASE_SERVICE_ROLE_KEY=<service-role key — salin dari .env tim, JANGAN dicommit>
SUPABASE_JWT_SECRET=<jwt secret project — salin dari .env tim>
